{% extends 'base.html.twig' %}

{% block title %} JOB category
{% endblock %}

{% block body %}
	<h1>{{ job.name }}</h1>
	<div class="row">
		<div class="col">
			Date de sortie:
			{% if job.startDate %}
				{{ job.finishDate.format("d/m/Y") }}
			{% else %}
				N/C
			{% endif %}